[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.137.2",
    "sprite-core",
    "uvicorn>=0.49.0",
]


[tool.uv.sources]
sprite-core = { path = "../packages/sprite_core", editable = true }
